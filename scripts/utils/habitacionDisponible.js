// import { hoyISO } from "./hoyISO"


// if (checkIn < hoyISO()) {
//   Swal.fire('Error', 'No puedes reservar en fechas pasadas', 'error')
//   return
// }

// if (checkOut <= checkIn) {
//   Swal.fire('Error', 'La salida debe ser posterior al ingreso', 'error')
//   return
// }
